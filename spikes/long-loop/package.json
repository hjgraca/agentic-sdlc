{
	"name": "spike-long-loop",
	"private": true,
	"type": "module",
	"scripts": {
		"spike": "rm -rf data && node loop.ts wake w1 && node loop.ts wake w2 && node loop.ts wake w2 && node loop.ts wake w3 && node loop.ts wake w4 && node loop.ts inspect"
	},
	"dependencies": {
		"@flue/runtime": "1.0.0-beta.5"
	},
	"devDependencies": {
		"@types/node": "^22",
		"typescript": "^5.9.3"
	}
}
