# Generic base — committed, no account-specific values. Deploy via an overlay
# (see ../local/) that patches in your registry, IAM role, and org URLs.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
